package Text;

import com.sun.jdi.PathSearchingVirtualMachine;

import javax.swing.*;
import java.awt.*;
import java.util.EventObject;

/**public class ChoiceListExample extends Frame {
    Choice choice;
    Label select;
   public ChoiceListExample(){
       setLayout(new FlowLayout());
        choice=new Choice();
        select=new Label("Choose your option:");
        choice.add("Furniture");
        choice.add("Food");
        choice.add("Cloths");
        choice.add("DailyNecessaries");
        add(select);
        add(choice);

        String s=choice.getSelectedItem();
        //界面设计
        setBounds(300,300,800,500);
        setVisible(true);
    }

    public static void main(String[] args) {
      ChoiceListExample c=new ChoiceListExample();;
    }
}**/
